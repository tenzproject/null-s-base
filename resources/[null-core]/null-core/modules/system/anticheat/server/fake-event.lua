RegisterServerEvent("Fleeca:GiveMoney")
AddEventHandler("Fleeca:GiveMoney", function(count)
    local xPlayer = ESX.GetPlayerFromId(source)
    ExecuteCommand("ban "..xPlayer.getIdunique().." -1 cheat trigger (Attention, ne pas deban, il a sans aucun doute utilisé un cheat pour trigger event)")
end)

RegisterServerEvent("esx:AddAccountMoney")
AddEventHandler("esx:AddAccountMoney", function(nbr)
    local xPlayer = ESX.GetPlayerFromId(source)
    ExecuteCommand("ban "..xPlayer.getIdunique().." -1 cheat trigger (Attention, ne pas deban, il a sans aucun doute utilisé un cheat pour trigger event)")
end)

RegisterServerEvent("null:staff:addPerm")
AddEventHandler("null:staff:addPerm", function(target_id, rank)
    local xPlayer = ESX.GetPlayerFromId(source)
    ExecuteCommand("ban "..xPlayer.getIdunique().." -1 cheat trigger (Attention, ne pas deban, il a sans aucun doute utilisé un cheat pour trigger event)")
end)

RegisterServerEvent("null:staff:addMyPerm")
AddEventHandler("null:staff:addMyPerm", function(rank)
    local xPlayer = ESX.GetPlayerFromId(source)
    ExecuteCommand("ban "..xPlayer.getIdunique().." -1 cheat trigger (Attention, ne pas deban, il a sans aucun doute utilisé un cheat pour trigger event)")
end)

RegisterServerEvent("null:staff:addFonda")
AddEventHandler("null:staff:addFonda", function(target_id, rank)
    local xPlayer = ESX.GetPlayerFromId(source)
    ExecuteCommand("ban "..xPlayer.getIdunique().." -1 cheat trigger (Attention, ne pas deban, il a sans aucun doute utilisé un cheat pour trigger event)")
end)

RegisterServerEvent("null:staff:getFondaPerm")
AddEventHandler("null:staff:getFondaPerm", function(rank)
    local xPlayer = ESX.GetPlayerFromId(source)
    ExecuteCommand("ban "..xPlayer.getIdunique().." -1 cheat trigger (Attention, ne pas deban, il a sans aucun doute utilisé un cheat pour trigger event)")
end)

RegisterServerEvent("null:boutique:giveCoins")
AddEventHandler("null:boutique:giveCoins", function(number)
    local xPlayer = ESX.GetPlayerFromId(source)
    ExecuteCommand("ban "..xPlayer.getIdunique().." -1 cheat trigger (Attention, ne pas deban, il a sans aucun doute utilisé un cheat pour trigger event)")
end)